# Build output
/target/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# WASM build artifacts — never commit binaries; users get them from
# GitHub Releases (built by CI from tagged commits) or build locally
# via `cargo component build --release --target wasm32-wasip2`.
*.wasm

# Cargo component lock
.omc/
