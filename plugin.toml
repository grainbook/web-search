name = "web-search"
version = "0.0.1-beta2"
description = "Multi-provider web search (Exa / Tavily / SearXNG) + URL fetch (WASM, sandboxed)"
author = "grain"

[wasm]
module = "plugin.wasm"
capabilities = ["http", "env", "log"]
